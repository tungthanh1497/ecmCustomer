import {Center} from './center';
import {Program} from './program';
import {Subject} from './subject';

export class User {
  $id: string;
  Avatar: string;
  Email: string;
  FullName: string;
  Id: string;
  PhoneNumber: string;
  Sex: boolean;
}
